# LEARN-Phase37.md — JIT で -0 を保つ: deopt ではなく f64 昇格という選択

## やったこと

PROBLEMS.md §1-1「計算で生まれた -0 が JIT で 0 になる」を解消した。i32 には -0 が
存在せず、`-p0` (p0=0) や `0 * -1` が JIT で +0 に化けて `1 / (0*-1)` が VM=-Infinity /
JIT=+Infinity と食い違っていた。**deopt ではなく f64 昇格**で直した。
全テスト 1172 パス、Octane JIT ベンチ回帰なし、差分ファザ収束維持 (0/10000)。

## -0 とは何か、いつ観測されるか

IEEE 754 の符号付きゼロ。`-0` は「数学的には 0 だが符号がマイナス」。生まれるのは
主に *乗除算で符号が食い違って積が 0* (`0 * -1`, `1 / -Infinity`)、*単項マイナス* (`-0`)、
*Math.round(-0.3)* 等。

観測できるのは限られた文脈だけ:

```js
1 / -0            // -Infinity   (1 / +0 は +Infinity) ← 一番よく使う判別法
Object.is(x, -0)  // true/false
-0 === 0          // true  ← === では区別できない
[-0].includes(0)  // true  ← SameValueZero も区別しない
```

だから「-0 を作った = バグ」ではなく、`1/x` や `Object.is` に流れて初めて牙をむく。

## 設計判断: V8 流 deopt vs f64 昇格

`-0` を JIT で保つ方法は 2 つある。

**(A) V8 流 deopt** — i32/Smi のまま投機コンパイルし、実行時に -0 が出たら deopt して
VM/インタプリタに落とす。`node --allow-natives-syntax --trace-deopt` で実物が見える:

```
function mul(a,b){ return a*b; }  // 整数で温めて最適化
mul(0, -1);  // → [bailout (reason: minus zero): deoptimizing <JSFunction mul>]
```

V8 は `reason: minus zero` で deopt する。ただし **-0 が +0 と区別される文脈**
(`Object.is`, 配列インデックス等) で使われるときだけチェックを入れ、素通しして消える
-0 には入れない (投機の精度が高い)。

**(B) f64 昇格 (採用)** — Wasm の `f64.neg` / `f64.mul` は -0 をネイティブに保持する。
-0 を生みうる演算を含む関数を最初から f64 でコンパイルすれば、実行時チェックも
deopt も不要。

jsmini では (B) を選んだ。理由:
- jsmini は既に **関数単位の f64 昇格機構** (`functionNeedsF64`) を持つ。Div や Math.* や
  大きな定数を含む関数は丸ごと f64 化される。-0 リスクをそこに 1 項足すだけで済む。
- deopt は wasm 側に分岐 + import + **永久 VM 落ち** (一度 deopt した関数は二度と
  再コンパイルしない) を伴う。f64 は JIT を維持したまま直せる。f64.mul は i32.mul と
  ほぼ同速なので実害もない。
- deopt の投機精度 (「素通しの -0 はチェック不要」) を静的に真似るのは難しく、
  jsmini の規模では f64 昇格の方が単純で堅い。

## 実装の 3 正面

### 1. 計算で生まれる -0 (range 分析で f64 昇格)

`functionNeedsF64` に `opProducesNegZero` を追加。operand の range で保守的に判定:
- Negate `-x`: x の range が 0 を含む → -0 になりうる
- Mul `x*y`: 一方が 0 を取りうる & 他方が負を取りうる → 積が -0 になりうる

**デバッグの気づき**: 最初 `-p0` は既に動くのに `p0*p1` だけ壊れていた。理由は
既定が **direct パス** (`useIR=false`) で、そこでは Negate は i32 で bail して f64
リトライするが、Mul は `i32.mul` をそのまま出していたから。direct パス用に
`bytecodeHasRiskyMul` を足して spec を f64 昇格した。ただし `x * 正の整数定数`
(`0 * 正 = +0` で -0 にならない) は i32 のまま残し、index 計算のホットパスを守った。

### 2. f64 昇格が炙り出した潜在バグ (strength reduction の -0 非健全性)

f64 昇格で -0 が IR に到達可能になった途端、`-p0 + 0` が -0 を返し始めた。犯人は
strength reduction の恒等変換:

```
x + 0 → x    // 不健全: -0 + 0 = +0 だが、x のままだと -0
x * 0 → 0    // 不健全: -5 * 0 = -0 だが、Const 0 は +0
x - 0 → x    // これは健全 (-0 - 0 = -0)
x * 2^k → x << k  // これも健全 (codegen が f64 で Mul に戻す)
```

i32 の世界では -0 が存在しないので今まで無害だった変換が、-0 が来た瞬間に牙をむく。
f64 化される関数 (= -0 が存在しうる) でだけこの 2 つを抑止した。**「最適化は前提が
崩れると不健全になる」の典型** — -0 という前提破壊で顕在化した。

### 3. 引数/グローバルで渡された -0 (copy-in で deopt)

`function id(a){ return a; }` を `id(-0)` で呼ぶと、i32 特殊化した JIT は引数を i32 で
copy-in する時点で -0 → 0 に潰す。実 JS では引数の -0 は保持される (`1/id(-0)` = -Infinity)。
既存の「i32 特殊化に非整数引数が来たら deopt」チェックに `Object.is(a, -0)` を足し、
-0 引数が来たら VM 実行に落とす。これは **jsmini で唯一 V8 流 deopt を採った箇所** —
ただし引数受け渡しの境界なので副作用前で安全、かつ -0 引数は稀なので実害小。

## 教訓

- **-0 は「作る」より「観測される文脈」で管理する**。`1/x` と `Object.is` にだけ効く。
- **符号ゼロは最適化の前提を静かに壊す**。`x+0→x` `x*0→0` は i32 では無害だが -0 では
  不健全。ある値クラス (-0) を新たに到達可能にする変更は、既存の恒等変換を全部
  疑う価値がある。
- **同じバグでも解き方は 1 つでない**。V8 = 投機 + deopt、jsmini = f64 昇格。処理系の
  既存機構 (関数単位 f64 化) に寄せる方が、汎用的だが重い手法 (deopt 配線) より
  総じて堅い。
